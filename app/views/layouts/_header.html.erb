<header id="aaa">
  <nav class="navbar navbar-default navbar-fixed-top bbb">
    <div class="container-fluid ccc">
      <div class="navbar-inner ddd">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#site-menu" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <% unless user_signed_in? %>
            <h1><%= link_to "人生を変える一冊", root_path, class: "logo" %></h1>
          <% else %>
            <h1><%= link_to "人生を変える一冊", books_path, class: "logo" %></h1>
          <% end %>
        </div>
        
        <div class="collapse navbar-collapse" id="site-menu">
          <ul class="nav navbar-nav navbar-right element">
            <% if user_signed_in? %>
              <li><%= link_to "読みたいランキング", rankings_index_path %></li>
              <li><%= link_to "My Page", current_user %></li>
              <li><%= link_to "ログアウト", destroy_user_session_path, method: "delete" %></li>
            <% else %>
              <li><%= link_to "ログイン", new_user_session_path %></li>
              <li><%= link_to "新規登録", new_user_registration_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </nav>
</header>







