<%= form_for(current_user.favorites.find_by(book_id: book.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "", class: "favorite_form_hidden" %>
  <button class="favorite_form_show"><i class="fa fa-heart" aria-hidden="true"></i></button>
<% end %>