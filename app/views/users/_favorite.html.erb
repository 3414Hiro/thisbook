<%= form_for(current_user.favorites.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :book_id, book.id %></div>
  <%= f.submit "", class: "favorite_form_hidden" %>
  <button class="favorite_form_show"><i class="fa fa-heart-o"></i></button>
<% end %>
